# Directory where converted EPUBs will be saved (host path)
# Defaults to ./output relative to the project directory
OUTPUT_DIR=./output

# Traefik reverse proxy (only needed if you're already running traefik with
# an external "proxy" network on this host — see README).
LOCAL_DOMAIN_NAME=example.com
PROJECT_SUB_DOMAIN=pdf2epub
